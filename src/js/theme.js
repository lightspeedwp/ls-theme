/**
 * Front-end entry point for ls-theme.
 *
 * Existing theme-specific scripts are still enqueued from PHP.
 * This file establishes the build pipeline entry for future bundled code.
 */

document.documentElement.classList.add( 'ls-theme-has-build' );